server {
    listen 80;
    listen [::]:80;

    location /api {
        proxy_pass http://server:5000;
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri /index.html;
    }
}