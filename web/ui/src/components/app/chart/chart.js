import React from "react";

import {
    createStyles
} from "@mantine/core"

const useStyles = ((theme) => ({
    chartBuilder: {

    },

    chartContainer: {
        
    }
}))

const ChartBuilder = ({  }) => {
    return (
        <div>

        </div>
    )
}

export default ChartBuilder