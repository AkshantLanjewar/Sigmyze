function ParseData(labels: string[], data: { label: string, data: any[] }[]) {
    for(let i = 0; i < labels.length; i++) {
        let label = labels[i]
        
        for(let x = 0; x < data.length; x++) {
            let point = data[x]
        }
    }
}

export { ParseData }